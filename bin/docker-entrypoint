#!/bin/bash -e

# If we are starting the web server, run db:prepare first.
# This works with the Thruster-wrapped `rails server` command.
if [[ "${@}" == *"rails server"* ]]; then
  ./bin/rails db:prepare
fi

exec "${@}"
