#!/bin/bash -e

# Run db:prepare before starting the web server (works for both rails server and puma)
if [[ "${@}" == *"rails server"* ]] || [[ "${@}" == *"puma"* ]]; then
  ./bin/rails db:prepare
fi

exec "${@}"
